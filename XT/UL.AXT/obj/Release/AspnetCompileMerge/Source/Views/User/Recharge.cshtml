@model Model.UserDTO
@{
    Layout = "~/Views/Shared/Main.cshtml";
}

<script src="~/Scripts/formValidator/formValidator-4.1.3.js"></script>
<script src="~/Scripts/formValidator/formValidatorRegex.js"></script>
<script type="text/javascript">
    $(function() {
        $("#btnSubmitCharge").click(function () {
            if (!$.formValidator.pageIsValid('1'))
                return false;

            window.location.href = "/User/PayRecharge?chargeNum=" + $("#rechargeNum").val() + "&userOpenId=" + getCookie("OpenId");
            //$.ajax({
            //    url: "/User/SubmitRecharge",
            //    type: "POST",
            //    data: {  userId: getCookie("UserId"), },
            //    //data:{payFee:$("#needPay").val(),userOpenId:getCookie("OpenId")},
            //    success: function (data) {
            //        if (data["Succeeded"]) {
            //            window.location.href = "/User/PayRecharge?lottery=" + lotterys + "&phone=" + tel + "&email=" + email + "&needPay=" + $("#needPay").text() + "&userOpenId=" + getCookie("OpenId") + "&orderId=" + data["ResultId"];
            //        }
            //    }
            //});
        });

        $.formValidator.initConfig({ validatorGroup: "1", formID: "form1", onError: function () { alert("请查看错误信息"); } });
        $("#rechargeNum").formValidator({ validatorGroup: "1", onShow: "", onCorrect: "", empty: false }).regexValidator({ regExp: "intege1", dataType: "enum", onError: "整数格式不正确" });

    });

   
</script>

@*<input id="rechargeNum" type="text" value=""/>
<input type="button" value="提交订单" id="btnSubmitCharge"/>*@

<div class="order_submit">
    @*<h3 class="fn">可提现金额：<i class="red">@Model.AccountBalance 元</i></h3>*@
    <ul>
        <li class="white_bg"><input type="text" class="block Ntext h30 lh30" id="rechargeNum"/><input type="hidden" value="请输入充值金额" /></li>
        </ul>
    <p class="h20 lh20 red"><div id="rechargeNumTip"></div> </p>
</div>
<div class="user_caseB white_bg oh">
    @*<p class="f20 margin_t10 fb">1-3个工作日处理完成</p>
    <p class="f12 gray6">银行处理可能会有延迟，具体以到账时间为准。</p>
    <p class="f12 red margin_t20" style="height:auto;">注意：为了你的账户安全，平台将在第一次提现成功后将更新并锁定该账户真实姓名。</p>*@
    <a href="#" class="button orange_bg tc block gray3_border br4" id="btnSubmitCharge">充值</a>
    <p class="f14 tc"></p>
</div>
@RenderPage("~/Views/Shared/_Footer.cshtml")